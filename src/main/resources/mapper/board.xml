<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.blog2.model.BoardRepository">
    <select id="findAll" resultType="shop.mtcoding.blog2.model.Board">
        select * from board_tb
    </select>

    <select id="findAllforList" resultType="shop.mtcoding.blog2.dto.board.BoardResp$BoardMainListDto">
      select b.id, b.title, ( select username from user_tb where id = b.user_id ) username from board_tb b 
    </select>

    <insert id="insertBoard">
      insert into board_tb ( title, content, user_id, created_at) values (#{title}, #{content}, #{userId}, now())
    </insert>
</mapper>